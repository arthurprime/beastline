import 'package:flutter/material.dart';

class LiveChallengePage extends StatelessWidget {
  const LiveChallengePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Live Challenge')),
      body: Center(child: Text('Live Challenge Page')),
    );
  }
}
